/* pages/chart/chart.wxss */
.all{
  position: relative;
  text-align: center;
}

.txt{
  position: relative;
  font-weight: 800;
}

.canvas {
  position: relative;
  left: 30rpx;
  width: 1000rpx;
  height: 700rpx;
  transform: scale(1)
}